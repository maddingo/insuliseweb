<?xml version="1.0" encoding="UTF-8" ?>
<solr>
  <str name="adminHandler">${adminHandler:org.apache.solr.handler.admin.CoreAdminHandler}</str>
  <int name="coreLoadThreads">${coreLoadThreads:3}</int>
  <str name="coreRootDirectory">${coreRootDirectory:}</str> <!-- usually solrHome  -->
  <str name="managementPath">${managementPath:}</str>
  <str name="sharedLib">${sharedLib:}</str>
  <str name="shareSchema">${shareSchema:false}</str>
  <int name="transientCacheSize">${transientCacheSize:150000}</int> <!-- ignored unless cores are defined with transient=true -->

  <!--solrcloud>
    <int name="distribUpdateConnTimeout">${distribUpdTimeout:15000}</int>
    <int name="distribUpdateSoTimeout">${distribUpdateTimeout:15000}</int>
    <int name="leaderVoteWait">${leaderVoteWait:15000}</int>
    <str name="host">${host:}</str>
    <str name="hostContext">${hostContext:solr}</str>
    <int name="hostPort">${jetty.port:8983}</int>
    <int name="zkClientTimeout">${zkClientTimeout:15000}</int>
    <str name="zkHost">${zkHost:}</str>
    <bool name="genericCoreNodeNames">${genericCoreNodeNames:true}</bool>
  </solrcloud-->


  <logging>
    <str name="class">${loggingClass:}</str>
    <str name="enabled">${loggingEnabled:}</str>
    <watcher>
      <int name="size">${loggingSize:1000000}</int>
      <int name="threshold">${loggingThreshold:10000}</int>
    </watcher>
  </logging>

  <shardHandlerFactory name="shardHandlerFactory" class="HttpShardHandlerFactory">
    <int name="socketTimeout">${socketTimeout:15000}</int>
    <int name="connTimeout">${socketTimeout:15000}</int>
  </shardHandlerFactory>

</solr>
